services:
  - type: web
    name: totalfix27x7-frontend
    env: static
    buildCommand: npm run build
    publishPath: ./dist

    routes:
      - type: rewrite
        source: /*
        destination: /index.html